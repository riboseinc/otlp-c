/* SPDX-License-Identifier: Apache-2.0 */
/*
 * Protobuf wire encoder — STUB.
 *
 * Phase 1 of the roadmap. The first thing the implementing agent
 * should build. The public API for this module is in
 * src/protobuf_encode.h (internal header).
 *
 * Hand-rolled. No protobuf library. The encoder only needs to
 * emit the wire types used by the OTLP messages (varint, fixed64,
 * fixed32, length-delimited). See docs/otlp-spec.md for the
 * message schemas.
 */
#include "protobuf_encode.h"

/* TODO Phase 1. */

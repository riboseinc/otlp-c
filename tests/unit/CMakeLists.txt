# SPDX-License-Identifier: BSD-3-Clause
#
# Targeted unit tests for individual module functions. Complements
# the property tests with specific known-answer assertions.

add_executable(otlp_unit_protobuf test_unit_protobuf.c)
target_link_libraries(otlp_unit_protobuf PRIVATE otlp_c)
target_include_directories(otlp_unit_protobuf PRIVATE
    ${CMAKE_SOURCE_DIR}/src)
add_test(NAME unit-protobuf
    COMMAND otlp_unit_protobuf)
set_tests_properties(unit-protobuf PROPERTIES LABELS "unit")

add_executable(otlp_unit_span test_unit_span.c)
target_link_libraries(otlp_unit_span PRIVATE otlp_c)
add_test(NAME unit-span
    COMMAND otlp_unit_span)
set_tests_properties(unit-span PROPERTIES LABELS "unit")

add_executable(otlp_unit_metric test_unit_metric.c)
target_link_libraries(otlp_unit_metric PRIVATE otlp_c)
target_include_directories(otlp_unit_metric PRIVATE
    ${CMAKE_SOURCE_DIR}/src)
add_test(NAME unit-metric
    COMMAND otlp_unit_metric)
set_tests_properties(unit-metric PROPERTIES LABELS "unit")

add_executable(otlp_unit_platform test_unit_platform.c)
target_link_libraries(otlp_unit_platform PRIVATE otlp_c)
target_include_directories(otlp_unit_platform PRIVATE
    ${CMAKE_SOURCE_DIR}/src)
add_test(NAME unit-platform
    COMMAND otlp_unit_platform)
set_tests_properties(unit-platform PROPERTIES LABELS "unit")

add_executable(otlp_unit_log test_unit_log.c)
target_link_libraries(otlp_unit_log PRIVATE otlp_c)
target_include_directories(otlp_unit_log PRIVATE
    ${CMAKE_SOURCE_DIR}/src)
add_test(NAME unit-log
    COMMAND otlp_unit_log)
set_tests_properties(unit-log PROPERTIES LABELS "unit")

# Known-answer wire test (v0.5.97): pins encoded field numbers and
# the schema tables against opentelemetry-proto literals — the
# independent reference otlp_schema.h must match.
add_executable(otlp_unit_wire_numbers test_unit_wire_numbers.c)
target_link_libraries(otlp_unit_wire_numbers PRIVATE otlp_c)
target_include_directories(otlp_unit_wire_numbers PRIVATE
    ${CMAKE_SOURCE_DIR}/src)
add_test(NAME unit-wire-numbers
    COMMAND otlp_unit_wire_numbers)
set_tests_properties(unit-wire-numbers PROPERTIES LABELS "unit")

# Golden vectors (v0.5.101): our encoders vs the reference
# opentelemetry-proto serialization, compared as canonical field
# trees. Payloads are generated by tests/golden/generate.py and
# embedded in golden_vectors.h — no runtime file dependency.
add_executable(otlp_unit_golden test_unit_golden.c)
target_link_libraries(otlp_unit_golden PRIVATE otlp_c)
target_include_directories(otlp_unit_golden PRIVATE
    ${CMAKE_SOURCE_DIR}/src
    ${CMAKE_SOURCE_DIR}/tests/golden)
add_test(NAME unit-golden
    COMMAND otlp_unit_golden)
set_tests_properties(unit-golden PROPERTIES LABELS "unit")

# UTF-8 boundary validation (v0.5.103): OTLP string fields must be
# valid UTF-8 — collectors reject whole batches otherwise.
add_executable(otlp_unit_utf8 test_unit_utf8.c)
target_link_libraries(otlp_unit_utf8 PRIVATE otlp_c)
target_include_directories(otlp_unit_utf8 PRIVATE
    ${CMAKE_SOURCE_DIR}/src)
add_test(NAME unit-utf8
    COMMAND otlp_unit_utf8)
set_tests_properties(unit-utf8 PROPERTIES LABELS "unit")

# otlp_strerror completeness pin (v0.5.105).
add_executable(otlp_unit_common test_unit_common.c)
target_link_libraries(otlp_unit_common PRIVATE otlp_c)
add_test(NAME unit-common
    COMMAND otlp_unit_common)
set_tests_properties(unit-common PROPERTIES LABELS "unit")

# Deep-clone round-trips (v0.6.2): the clone/copy arms for
# everything richer than scalar attributes had never executed.
add_executable(otlp_unit_clone test_unit_clone.c)
target_link_libraries(otlp_unit_clone PRIVATE otlp_c)
target_include_directories(otlp_unit_clone PRIVATE
    ${CMAKE_SOURCE_DIR}/src)
add_test(NAME unit-clone
    COMMAND otlp_unit_clone)
set_tests_properties(unit-clone PROPERTIES LABELS "unit")

# HTTP response wire-format parser: pure byte fixtures — no
# sockets/threads, runs on every platform including MSVC (v0.6.11).
add_executable(otlp_unit_http_response_parser test_unit_http_response_parser.c)
target_link_libraries(otlp_unit_http_response_parser PRIVATE otlp_c)
target_include_directories(otlp_unit_http_response_parser PRIVATE
    ${CMAKE_SOURCE_DIR}/src)
add_test(NAME unit-http-response-parser
    COMMAND otlp_unit_http_response_parser)
set_tests_properties(unit-http-response-parser PROPERTIES LABELS "unit")

# OTel standard env-var configuration (v0.7.0): pure parsers +
# one getenv-driven pass; portable (MSVC _putenv_s fallback).
add_executable(otlp_unit_env_config test_unit_env_config.c)
target_link_libraries(otlp_unit_env_config PRIVATE otlp_c)
target_include_directories(otlp_unit_env_config PRIVATE
    ${CMAKE_SOURCE_DIR}/src)
add_test(NAME unit-env-config
    COMMAND otlp_unit_env_config)
set_tests_properties(unit-env-config PROPERTIES LABELS "unit")

# Sync-flush engine (exporter_sync.c, v1.1.2): guards, null
# transport, transport-error accounting - public surface only.
add_executable(otlp_unit_exporter_sync test_unit_exporter_sync.c)
target_link_libraries(otlp_unit_exporter_sync PRIVATE otlp_c)
target_include_directories(otlp_unit_exporter_sync PRIVATE
    ${CMAKE_SOURCE_DIR}/src)
add_test(NAME unit-exporter-sync
    COMMAND otlp_unit_exporter_sync)
set_tests_properties(unit-exporter-sync PROPERTIES LABELS "unit")
